# develop

```bash
# install packages
yarn install
# start the database
cd dev && docker-compose up -d
# run apps
yarn dev
```

# to build

```bash
# install packages
yarn install
# start the database
cd dev && docker-compose up -d
# run apps
yarn build
```
