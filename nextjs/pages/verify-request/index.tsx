import Layout from '../../components/layout/CardLayout';

export default function VerifyRequest({}: {}) {
  return (
    <Layout header="Check your email">
      We emailed a magic link to your email address. Click the link to log in or
      sign up
    </Layout>
  );
}
