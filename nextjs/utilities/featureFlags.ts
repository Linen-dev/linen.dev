export const isStripeEnabled = false;
export const isRedirectToNewOnboardingEnabled = false;
export const isNewOnboardingButtonEnabled = false;
export const isMetricEnabled = false;

const featureFlags = {
  isStripeEnabled,
  isMetricEnabled,
};

export default featureFlags;
