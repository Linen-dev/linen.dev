export const patterns = {
  communityName: /^[A-Za-z][A-Za-z0-9-_ ']+/, // starts with letter, allowed number, hyphen, underscore, space, single quote
  communityPath: /^[a-z][a-z0-9-_]+/, // starts with lower case letter, allowed number, hyphen, underscore
  channelName: /^[a-z][a-z0-9-_]+/, // starts with letter, allowed number, hyphen, underscore
};
