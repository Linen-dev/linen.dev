{
  "name": "@linen/types",
  "private": true,
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "rm -rf dist && yarn tsup --watch",
    "build": "rm -rf dist && yarn tsup"
  },
  "dependencies": {
    "typescript": "^4.9.3",
    "tsup": "^6.5.0",
    "zod": "^3.20.2"
  }
}
