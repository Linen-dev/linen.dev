{
  "name": "@linen/types",
  "private": true,
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "sideEffect": false,
  "scripts": {
    "dev": "rimraf dist && yarn tsup --watch",
    "build": "rimraf dist && yarn tsup"
  },
  "dependencies": {
    "@linen/database": "*",
    "express": "^4.18.2",
    "tsup": "^6.7.0",
    "typescript": "^5.1.3",
    "zod": "^3.21.4"
  },
  "devDependencies": {
    "@types/express": "^4.17.17"
  }
}
