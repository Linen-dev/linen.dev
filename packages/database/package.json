{
  "name": "@linen/database",
  "private": true,
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "generate": "yarn prisma generate",
    "migrate": "yarn prisma migrate",
    "build": "yarn generate && rimraf dist && yarn tsc"
  },
  "dependencies": {
    "@prisma/client": "4.5.0",
    "axios": "^0.27.2"
  },
  "devDependencies": {
    "prisma": "4.5.0",
    "typescript": "^5.1.3"
  },
  "browser": {
    "fs": false,
    "os": false
  }
}
